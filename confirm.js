const cfg=window.FIERA_CONFIG;const sb=supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);const token=new URLSearchParams(location.search).get('t');const box=document.querySelector('#box');
const fmt=d=>new Intl.DateTimeFormat('it-IT',{weekday:'long',day:'numeric',month:'long'}).format(new Date(d+'T12:00:00'));
async function load(){if(!token){box.innerHTML='<h1>Link non valido</h1><p class="muted">Manca il codice di conferma.</p>';return}const {data,error}=await sb.rpc('get_confirmation',{p_token:token});if(error||!data){box.innerHTML='<h1>Link non valido</h1><p class="muted">Il servizio non è stato trovato.</p>';return}const a=Array.isArray(data)?data[0]:data;if(!a){box.innerHTML='<h1>Link non valido</h1>';return}
box.innerHTML=`<div class="status ${a.confirmation_status==='Confermato'?'s-green':a.confirmation_status==='Non posso'?'s-red':'s-orange'}">${a.confirmation_status||'Da confermare'}</div><h1 style="font-size:36px;margin-top:14px">${fmt(a.event_date)}</h1><p><b>${a.pavilion_name}</b><br>${a.role}</p><p class="muted">${a.full_name}, confermi la tua presenza?</p><div class="grid grid2 section"><button class="btn btn-primary" id="yes">Confermo</button><button class="btn btn-secondary" id="no">Non posso</button></div><div id="msg" class="section"></div>`;
document.querySelector('#yes').onclick=()=>respond('Confermato');document.querySelector('#no').onclick=()=>respond('Non posso')}
async function respond(status){const {error}=await sb.rpc('respond_confirmation',{p_token:token,p_status:status});document.querySelector('#msg').innerHTML=error?`<div class="notice danger">${error.message}</div>`:`<div class="notice success">Risposta registrata: <b>${status}</b>.</div>`;if(!error)setTimeout(load,600)}load();
